{
  "name": "leetflash-deploy",
  "version": "0.1.0",
  "engines": {
    "node": "14.17.6"
  },
  "scripts": {
    "build": "npm install --force --prefix client --production=false && npm install --prefix client react@latest react-dom@latest && npm run build --prefix client && npm install --prefix server --production=false && npm run build --prefix server"
  }
}